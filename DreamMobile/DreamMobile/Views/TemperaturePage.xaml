<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewModel="clr-namespace:DreamMobile.ViewModels"
             x:Class="DreamMobile.Views.TemperaturePage"
             Title="Temperature and Humidity">

    <ContentPage.BindingContext>
        <viewModel:TemperatureViewModel/>
    </ContentPage.BindingContext>

    <StackLayout>
        <Label Text="Sensors show:" FontSize="Medium" Margin="10"/>

        <Label Text="Temperature" 
               FontSize="Large"
               VerticalOptions="Center"
               Margin="10"
               x:Name="TemperatureTextLabel"/>

        <Label x:Name="TemperatureLabel"
               FontSize="Large"
               VerticalOptions="Center"
               Margin="10"/>

        <Label Text="Humidity"
               x:Name="HumidityTextLabel"
               FontSize="Large"
               BackgroundColor="Aqua"
               VerticalOptions="Center"
               Margin="10"/>

        <Label x:Name="HumidityLabel" 
               FontSize="Large"
               BackgroundColor="Aqua"
               VerticalOptions="Center"
               Margin="10"/>

        

    </StackLayout>

</ContentPage>