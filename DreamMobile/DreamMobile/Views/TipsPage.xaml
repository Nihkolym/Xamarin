<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:DreamMobile.ViewModels"
             x:Class="DreamMobile.Views.TipsPage"
             Title="TipsPage">
    <ContentPage.BindingContext>
        <vm:TipsViewModel/>
    </ContentPage.BindingContext>
    <ContentPage.Content>
        <StackLayout>
            <Label Text="{Binding Greeting}"
                VerticalOptions="Center" FontSize="Medium" />
            <Label Text="DesiresTemperature" 
                   FontSize="Large" 
                   BackgroundColor="Red"
                   VerticalOptions="Center"/>
            
            <Label Text="{Binding DesiresTemperature}"
                   FontSize="Large" 
                   BackgroundColor="Red" 
                   VerticalOptions="Center"/>
            
            <Label Text="DesiresHumidity" 
                   FontSize="Large"
                   BackgroundColor="Aqua"
                   VerticalOptions="Center"/>
            
            <Label Text="{Binding DesiresHumidity}"
                   FontSize="Large"
                   BackgroundColor="Aqua" 
                   VerticalOptions="Center"/>
            
        </StackLayout>
    </ContentPage.Content>
</ContentPage>